# ── Streamlit Cloud runtime dependencies ──────────────────────────────────────
# Only packages actually imported by the app (app/ directory).
# Pipeline-only deps (geopandas, folium, scipy, sklearn, matplotlib, seaborn,
# jupyter, ipykernel, pyyaml, openpyxl, xlrd) are intentionally excluded.

streamlit>=1.35.0
pandas>=2.0.0
numpy>=1.26.0
plotly>=5.20.0
requests>=2.28.0
yfinance>=0.2.40
pyarrow>=14.0.0
pyyaml>=6.0
